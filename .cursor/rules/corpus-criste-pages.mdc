---
description: Page creation workflow and template mapping for CorpusCriste
globs: app/**/page.tsx, specs/content/**
alwaysApply: false
---

# CorpusCriste — Pages

## Template mapping

| Page | Template | JSON |
|------|----------|------|
| Home | ContentPageTemplate | home.json |
| Imagens (index) | ListingPageTemplate | imagens.json |
| Nossa Senhora Auxiliadora | ContentPageTemplate compact | nossa-senhora-auxiliadora.json |
| **Imagens template** | ContentPageTemplate compact | imagens-template.json |
| Ministérios (index) | ListingPageTemplate | ministerios.json |
| DEA Ajuda | Custom (do not copy) | dea-ajuda.json |
| **Ministérios template** | ContentPageTemplate compact | ministerios-template.json |

## Minimal page.tsx (copy exactly)

```tsx
import type { Metadata } from 'next'
import { ContentPageTemplate } from '@/components/content/ContentPageTemplate'
import { loadContent } from '@/lib/specs/loader'

const content = loadContent('your-slug')

export const metadata: Metadata = {
  title: content.meta.title,
  description: content.meta.description,
}

export default function Page() {
  return <ContentPageTemplate content={content} compact />
}
```

## New page checklist

1. Copy JSON from `imagens-template.json` or `ministerios-template.json`
2. Copy `app/imagens/template/page.tsx` (or ministerios equivalent)
3. Register slug in `lib/specs/types.ts` + `lib/specs/loader.ts`
4. Add route in `specs/routes.json` with `parent`
5. Add listing card in `imagens.json` or `ministerios.json`
6. Add redirect in `vercel.json` if renaming legacy URLs
7. Run `npm run test:specs` and `npm run test:e2e`
